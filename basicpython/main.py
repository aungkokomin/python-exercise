# from utils.text import slugify
# print(slugify("Hello World!"))

from utils.mathx import clamp,chunks
print(clamp(10, 1, 5))  # Example usage of clamp
print(list(chunks(range(10), 3)))  # Example usage of chunks